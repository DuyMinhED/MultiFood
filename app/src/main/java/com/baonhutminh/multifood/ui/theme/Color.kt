package com.baonhutminh.multifood.ui.theme

import androidx.compose.ui.graphics.Color

// --- BẢNG MÀU CHẾ ĐỘ SÁNG (LIGHT THEME) ---
val md_theme_light_primary = Color(0xFFFF6D00) // Cam đậm (Màu chính)
val md_theme_light_onPrimary = Color(0xFFFFFFFF) // Chữ trắng trên nền cam
val md_theme_light_primaryContainer = Color(0xFFFFD180) // Cam nhạt (Nền button phụ)
val md_theme_light_onPrimaryContainer = Color(0xFFE65100) // Chữ đậm trên nền cam nhạt

val md_theme_light_secondary = Color(0xFFBF360C) // Cam đất/Nâu đỏ (Dùng cho icon phụ)
val md_theme_light_onSecondary = Color(0xFFFFFFFF)

val md_theme_light_background = Color(0xFFFAFAFA) // Xám cực nhạt (Nền App)
val md_theme_light_surface = Color(0xFFFFFFFF)    // Trắng tinh (Nền Card/Item)
val md_theme_light_onSurface = Color(0xFF191C1C)  // Chữ đen chính

val md_theme_light_error = Color(0xFFBA1A1A)      // Màu đỏ báo lỗi

// --- BẢNG MÀU CHẾ ĐỘ TỐI (DARK THEME) ---
// Chế độ tối cần giảm độ rực của màu cam để đỡ chói mắt
val md_theme_dark_primary = Color(0xFFFFB74D) // Cam phấn
val md_theme_dark_onPrimary = Color(0xFF4E2600) // Chữ nâu đậm trên nền cam
val md_theme_dark_primaryContainer = Color(0xFFE65100)
val md_theme_dark_onPrimaryContainer = Color(0xFFFFD180)

val md_theme_dark_secondary = Color(0xFFFFCCBC)
val md_theme_dark_onSecondary = Color(0xFF4E2600)

val md_theme_dark_background = Color(0xFF191C1C) // Đen xám (Nền App tối)
val md_theme_dark_surface = Color(0xFF202323)    // Xám đậm (Nền Card tối)
val md_theme_dark_onSurface = Color(0xFFE1E3E3)  // Chữ trắng